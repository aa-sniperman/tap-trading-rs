# Apply these settings in postgresql.conf or your managed PostgreSQL parameter group.
# Values may need tuning depending on connector count and write volume.

wal_level = logical
max_replication_slots = 8
max_wal_senders = 8
max_slot_wal_keep_size = 4096
wal_sender_timeout = 60s
  